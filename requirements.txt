playwright>=1.40
# easyocr is used by scrapers/newark_community_center.py to OCR the Silliman
# Center weekly schedule poster from Facebook. Pulls in torch + cv2 (~600MB
# total). If you don't need the Newark scraper, comment this out.
easyocr>=1.7
# beautifulsoup4 parses Facebook page HTML when the Newark scraper runs in
# the SCRAPERAPI_KEY=... mode (proxied fetch on GitHub Actions). The
# Playwright local path doesn't need it.
beautifulsoup4>=4.12
